// Personal OS scheduler override: strict visible to-do order allocation rules.
(function(){
  const VERSION='visible-dom-priority-v12';
  const STATE_KEY='personalOS.schedule.v5';
  const TASKS_KEY='personalOS.tasks.v1';
  const DAY_LABELS=['Monday','Tuesday','Wednesday','Thursday','Friday','Saturday','Sunday'];
  const DAY_WORDS=[['monday','mon',0],['tuesday','tue',1],['tuesday','tues',1],['wednesday','wed',2],['thursday','thu',3],['thursday','thur',3],['thursday','thurs',3],['friday','fri',4],['saturday','sat',5],['sunday','sun',6],['tirsdag','tir',1]];
  let lastVisibleRows=[];
  function pad(n){return String(n).padStart(2,'0')}
  function hm(m){return pad(Math.floor(m/60))+':'+pad(Math.round(m%60))}
  function mins(s){s=String(s||'00:00');const m=s.match(/(\d{1,2}):(\d{2})/);return m?Number(m[1])*60+Number(m[2]):0}
  function ymd(d){return d.getFullYear()+'-'+pad(d.getMonth()+1)+'-'+pad(d.getDate())}
  function addDays(d,n){const x=new Date(d);x.setDate(x.getDate()+n);return x}
  function monday(d){const x=new Date(d);const day=(x.getDay()+6)%7;x.setHours(0,0,0,0);x.setDate(x.getDate()-day);return x}
  function scheduleMonday(){return monday(new Date())}
  function todayIndex(){return (new Date().getDay()+6)%7}
  function nowMin(){const d=new Date();return d.getHours()*60+d.getMinutes()+d.getSeconds()/60}
  function nextFive(){const n=nowMin();let x=Math.ceil(n/5)*5;if(x<=n)x+=5;return x}
  function read(k,f){try{return JSON.parse(localStorage.getItem(k)||'')||f}catch(e){return f}}
  function write(k,v){localStorage.setItem(k,JSON.stringify(v))}
  function toastMsg(msg){try{if(typeof toast==='function'){toast(msg);return}}catch(e){}console.log(msg)}
  function exposeState(s){try{window.state=s}catch(e){}try{state=s}catch(e){}}
  function setStateObj(s){write(STATE_KEY,s);exposeState(s)}
  function getStateObj(){const s=read(STATE_KEY,{custom:[]});if(!Array.isArray(s.custom))s.custom=[];return s}
  function getTasks(){const arr=read(TASKS_KEY,[]);return Array.isArray(arr)?arr:[]}
  function saveTasks(arr){write(TASKS_KEY,arr);try{window.tasks=arr}catch(e){}try{tasks=arr}catch(e){}}
  function uniq(arr){return arr.filter((x,i,a)=>x&&a.indexOf(x)===i)}
  function cleanText(s){return String(s||'').replace(/\s+/g,' ').trim()}
  function parseText(s){return cleanText(String(s||'').replace(/[\u2010-\u2015]/g,'-'))}
  function overlaps(a,b){return a&&b&&a.date===b.date&&mins(a.start)<mins(b.end)&&mins(a.end)>mins(b.start)}
  function isGeneratedBlock(c){return !!(c&&(c.aiCreated||c.aiDetails||Array.isArray(c.taskIds)||Array.isArray(c.taskTexts)))}
  function getVisibleTaskRows(){const rows=[],seen=new Set();['#todoView #taskBody tr','#taskBody tr','#todoView .todoRow'].forEach(sel=>{document.querySelectorAll(sel).forEach(row=>{if(seen.has(row))return;if(!row.querySelector('.taskTextInput,.taskPill,textarea,input.cellInput,input[type=text]'))return;seen.add(row);rows.push(row)})});return rows}
  function rowText(row){const el=row.querySelector('.taskTextInput,.taskPill,textarea,input.cellInput:not([type=checkbox]),input[type=text]');return cleanText(el?String(el.value!=null?el.value:el.textContent||''):'')}
  function rowDone(row){const cb=row.querySelector('.taskCheck,input[type=checkbox]');return !!(cb&&cb.checked)}
  function rowDay(row){const el=row.querySelector('.cellSelect,select');return cleanText(el?String(el.value||''):'')}
  function exactTaskMatch(tasks,text,used){return tasks.find(t=>t&&!used.has(t)&&cleanText(t.text||t.title||'')===text)||null}
  function extractVisibleTasks(tasks){const rows=getVisibleTaskRows(),used=new Set();lastVisibleRows=[];rows.forEach((row,order)=>{const id=(row.dataset&&row.dataset.id)||row.getAttribute('data-id')||'',text=rowText(row);if(!text)return;const done=rowDone(row),day=rowDay(row);let task=null;if(id)task=tasks.find(t=>t&&t.id===id)||null;if(!task)task=exactTaskMatch(tasks,text,used);if(!task&&tasks[order]&&!used.has(tasks[order]))task=tasks[order];if(!task){task={id:id||('visible-'+Date.now()+'-'+order),done:false,text,day:'',area:'Personal',createdAt:new Date().toISOString()};tasks.push(task)}used.add(task);if(id&&!task.id)task.id=id;task.text=text;task.done=done;if(day||task.day!=null)task.day=day;lastVisibleRows.push({order,row,task,id:task.id||id||('task-'+order),text,done,day})});return lastVisibleRows}
  function syncTasksToVisibleOrder(tasks){const visible=lastVisibleRows.map(r=>r.task).filter(Boolean),seen=new Set(visible),ordered=visible.concat(tasks.filter(t=>!seen.has(t)));saveTasks(ordered);return ordered}
  function forceDomTaskOrder(){const body=document.getElementById('taskBody');if(!body)return;const tasks=getTasks(),byId={};[...body.querySelectorAll('tr')].forEach(row=>{const id=row.dataset&&row.dataset.id;if(id)byId[id]=row});tasks.forEach(t=>{if(t&&t.id&&byId[t.id])body.appendChild(byId[t.id])})}
  function installTaskReorder(){
    if(!document.getElementById('pos-task-order-style')){const s=document.createElement('style');s.id='pos-task-order-style';s.textContent='#todoView .dragCell{cursor:grab!important;user-select:none!important}#todoView tr.todoRow.dropAbove{outline:1px solid #9b6cff!important;outline-offset:-2px!important}';document.head.appendChild(s)}
    document.querySelectorAll('#todoView #taskBody tr').forEach(row=>{row.draggable=false;const h=row.querySelector('.dragCell');if(h)h.draggable=true});
    if(window.__posTaskReorderInstalled)return;
    window.__posTaskReorderInstalled=true;
    let draggedId='';
    document.addEventListener('dragstart',e=>{const handle=e.target&&e.target.closest&&e.target.closest('#todoView #taskBody .dragCell');if(!handle){draggedId='';return}const row=handle.closest('tr');if(!row)return;draggedId=(row.dataset&&row.dataset.id)||'';if(e.dataTransfer)e.dataTransfer.setData('text/plain',draggedId);row.classList.add('dragging')},true);
    document.addEventListener('dragend',()=>{document.querySelectorAll('#todoView #taskBody tr.dragging,#todoView #taskBody tr.dropAbove').forEach(x=>x.classList.remove('dragging','dropAbove'));draggedId=''},true);
    document.addEventListener('dragover',e=>{if(!draggedId)return;const row=e.target&&e.target.closest&&e.target.closest('#todoView #taskBody tr');if(!row)return;e.preventDefault();row.classList.add('dropAbove')},true);
    document.addEventListener('dragleave',e=>{const row=e.target&&e.target.closest&&e.target.closest('#todoView #taskBody tr');if(row)row.classList.remove('dropAbove')},true);
    document.addEventListener('drop',e=>{if(!draggedId)return;const row=e.target&&e.target.closest&&e.target.closest('#todoView #taskBody tr');if(!row)return;e.preventDefault();e.stopPropagation();if(e.stopImmediatePropagation)e.stopImmediatePropagation();const from=(e.dataTransfer&&e.dataTransfer.getData('text/plain'))||draggedId,to=(row.dataset&&row.dataset.id)||'';if(!from||!to||from===to)return;let arr=getTasks();const moving=arr.find(t=>t&&t.id===from);if(!moving)return;arr=arr.filter(t=>!(t&&t.id===from));const idx=arr.findIndex(t=>t&&t.id===to);arr.splice(idx<0?arr.length:idx,0,moving);saveTasks(arr);forceDomTaskOrder();try{if(typeof renderProductivity==='function')renderProductivity()}catch(err){}},true)
  }
  function parseExactRange(text){const s=parseText(text),m=s.match(/(?:^|[^\d])(\d{1,2})(?:[.:](\d{2}))?\s*-\s*(\d{1,2})(?:[.:](\d{2}))?(?!\d)/);if(!m)return null;const start=Number(m[1])*60+Number(m[2]||0),end=Number(m[3])*60+Number(m[4]||0);return start>=0&&end>start&&end<=1440?{start,end}:null}
  function parseDays(text){const s=parseText(text).toLowerCase(),found=[];function add(i){if(!found.includes(i))found.push(i)}if(/\btoday\b/.test(s))add(todayIndex());if(/\btomorrow\b/.test(s))add((todayIndex()+1)%7);DAY_WORDS.forEach(([full,short,idx])=>{if(new RegExp('\\b('+full+'|'+short+')\\b').test(s))add(idx)});return found}
  function parseSelectedDays(day){if(!day||/later|select/i.test(String(day)))return[];return parseDays(day)}
  function parseDuration(text){const raw=parseText(text),range=parseExactRange(raw);if(range)return range.end-range.start;const parens=[...raw.matchAll(/\(([^)]*)\)/g)].map(m=>m[1]).join(' '),target=(parens||raw).toLowerCase();let total=0;[...target.matchAll(/(\d+(?:\.\d+)?)\s*(h|hr|hrs|hour|hours)\b/g)].forEach(m=>{total+=Math.round(Number(m[1])*60)});[...target.matchAll(/(\d+)\s*(m|min|mins|minute|minutes)\b/g)].forEach(m=>{total+=Number(m[1])});if(!total&&parens&&/^\s*\d+\s*$/.test(parens))total=Number(parens.trim());return total||null}
  function cleanTitle(text){return cleanText(String(text||'').replace(/\([^)]*\)/g,'').replace(/\b(mon|monday|tue|tues|tuesday|wed|wednesday|thu|thur|thurs|thursday|fri|friday|sat|saturday|sun|sunday|today|tomorrow|tir|tirsdag)\b/ig,'').replace(/\b\d{1,2}(?:[.:]\d{2})?\s*[\-\u2010-\u2015]\s*\d{1,2}(?:[.:]\d{2})?\b/g,''))}
  function shortTitle(text){const s=cleanTitle(text)||'Task';if(s.length<=25)return s;const words=s.split(/\s+/);let out='';for(const w of words){const test=(out?out+' ':'')+w;if(test.length>25)break;out=test}return out||s.slice(0,25)}
  function typeFromText(text){return /(agency|client|sop|strategy|business|vinted|product|research|sales|marketing|money|work|shift|tok|commentary|meta|cro)/i.test(String(text||''))?'business':'personal'}
  function buildTimedItems(visibleRows){return visibleRows.map((r,i)=>{const text=r.text,range=parseExactRange(text),minutes=parseDuration(text),textDays=parseDays(text),selectedDays=range?parseSelectedDays(r.day):[],days=textDays.length?textDays:selectedDays;return{order:r.order,task:r.task,id:r.id||('task-'+i),text,done:!!r.done,minutes,remaining:minutes||0,days,range,title:shortTitle(text),type:typeFromText(text)}}).filter(x=>x.text&&x.text.toLowerCase()!=='new task'&&!x.done&&x.minutes!=null)}
  function makePlan(entries,minutes,base,opts){opts=opts||{};const texts=uniq(entries.map(e=>e.item.text)),ids=uniq(entries.map(e=>e.item.id));return{entries,items:entries.map(e=>e.item),taskTexts:texts,taskIds:ids,minutes,title:opts.title||base.title,type:entries.some(e=>e.item.type==='business')?'business':base.type,days:opts.days||[],range:opts.range||null,grouped:entries.length>1}}
  function buildPlansStrictVisibleOrder(items){const plans=[];for(let i=0;i<items.length;i++){const item=items[i];if(item.remaining<=0)continue;const dayList=item.days.length?item.days:[null];if(item.days.length||item.range){dayList.forEach(di=>plans.push(makePlan([{item,minutes:item.minutes}],item.minutes,item,{days:di==null?[]:[di],range:item.range,title:item.title})));item.remaining=0;continue}if(item.remaining>=45){const minutes=item.remaining;plans.push(makePlan([{item,minutes}],minutes,item,{title:item.title}));item.remaining=0;continue}const entries=[{item,minutes:item.remaining}];let total=item.remaining;item.remaining=0;let j=i+1;while(total<35&&j<items.length){const next=items[j];if(next.remaining<=0){j++;continue}if(next.days.length||next.range)break;entries.push({item:next,minutes:next.remaining});total+=next.remaining;next.remaining=0;j++}if(total>=35)plans.push(makePlan(entries,Math.max(45,total),item,{title:entries.length>1?'Grouped tasks':item.title}))}return plans}
  function eventIntervalsFromDom(dayIndex){const out=[],day=[...document.querySelectorAll('#scheduleView .day')][dayIndex];if(!day)return out;day.querySelectorAll('.event').forEach(el=>{const txt=(el.querySelector('.time')||{}).textContent||'',title=(el.querySelector('.title')||{}).textContent||'',m=txt.match(/(\d{1,2}:\d{2})\s*-\s*(\d{1,2}:\d{2})/);if(m)out.push({start:mins(m[1]),end:mins(m[2]),title,type:[...el.classList].join(' '),source:''})});return out}
  function blocksForDate(date,dayIndex){try{if(typeof blocksFor==='function'){const d=new Date(date+'T00:00:00'),w=typeof isoWeek==='function'?isoWeek(d):1;return(blocksFor(d,dayIndex,w)||[]).map(b=>({start:mins(b.start),end:mins(b.end),title:b.title||'',type:b.type||'',source:b.source||'',aiCreated:!!b.aiCreated}))}}catch(e){}return eventIntervalsFromDom(dayIndex)}
  function isSchoolBlock(b){return /school/i.test(b.type||'')||/\b2i\b|HL|SL|TOK|CAS|bio|psy|daA|enB|maAI|diagram|class|relationships|school/i.test(String(b.title||''))}
  function isProtectedBlock(b){if(isGeneratedBlock(b))return false;if(b.source==='custom')return true;if(isSchoolBlock(b))return true;return/routine|evening|wind|work|trip/i.test(String((b.type||'')+' '+(b.title||'')))}
  function merge(list){const a=list.filter(x=>x.end>x.start).sort((x,y)=>x.start-y.start),out=[];for(const x of a){const last=out[out.length-1];if(last&&x.start<=last.end)last.end=Math.max(last.end,x.end);else out.push({start:x.start,end:x.end})}return out}
  function subtract(base,busy){let free=[base];for(const b of merge(busy)){const next=[];for(const f of free){if(b.end<=f.start||b.start>=f.end)next.push(f);else{if(b.start>f.start)next.push({start:f.start,end:b.start});if(b.end<f.end)next.push({start:b.end,end:f.end})}}free=next}return free.filter(f=>f.end-f.start>=45)}
  function getProtectedIntervals(date,dayIndex,state,reserved){const busy=[],blocks=blocksForDate(date,dayIndex),school=blocks.filter(isSchoolBlock);if(school.length)busy.push({start:Math.min(...school.map(x=>x.start)),end:Math.max(...school.map(x=>x.end))});blocks.filter(b=>!isSchoolBlock(b)&&isProtectedBlock(b)).forEach(b=>busy.push({start:b.start,end:b.end}));(state.custom||[]).forEach(c=>{if(c.date===date&&!isGeneratedBlock(c))busy.push({start:mins(c.start),end:mins(c.end)})});(reserved[date]||[]).forEach(r=>busy.push(r));return busy}
  function freeFor(date,dayIndex,state,reserved){const today=ymd(new Date());let start=405,end=1350;if(date<today)return[];if(date===today){if(dayIndex<todayIndex())return[];start=Math.max(start,nextFive())}return subtract({start,end},getProtectedIntervals(date,dayIndex,state,reserved))}
  function reserve(reserved,slot){reserved[slot.date]=reserved[slot.date]||[];reserved[slot.date].push({start:slot.start,end:slot.end})}
  function unreserve(reserved,parts){parts.forEach(p=>{reserved[p.date]=(reserved[p.date]||[]).filter(r=>!(r.start===p.start&&r.end===p.end))})}
  function baseDateForDay(dayIndex){const start=scheduleMonday();let add=dayIndex;if(dayIndex<todayIndex())add+=7;return addDays(start,add)}
  function findExactSlot(dayIndex,range){const d=baseDateForDay(dayIndex);return{date:ymd(d),dayIndex,start:range.start,end:range.end}}
  function chooseChunk(remain,available){if(available<45)return 0;if(remain<=available)return remain>=45?remain:0;let chunk=available;const leftover=remain-chunk;if(leftover>0&&leftover<45)chunk=remain-45;return chunk>=45&&chunk<=available?chunk:0}
  function findChronologicalChunks(minutes,state,reserved,allowedDay){let remain=minutes;const parts=[],start=scheduleMonday();for(let offset=0;offset<364&&remain>0;offset++){const d=addDays(start,offset),date=ymd(d),di=(d.getDay()+6)%7;if(allowedDay!=null&&di!==allowedDay)continue;const free=freeFor(date,di,state,reserved);for(const f of free){if(remain<=0)break;const chunk=chooseChunk(remain,f.end-f.start);if(!chunk)continue;const part={date,dayIndex:di,start:f.start,end:f.start+chunk};parts.push(part);reserve(reserved,part);remain-=chunk}}if(remain===0)return parts;unreserve(reserved,parts);return[]}
  function placePlan(plan,state,reserved){if(plan.range&&plan.days.length){const slot=findExactSlot(plan.days[0],plan.range);if(!slot)return[];reserve(reserved,slot);return[slot]}if(plan.days.length)return findChronologicalChunks(plan.minutes,state,reserved,plan.days[0]);return findChronologicalChunks(plan.minutes,state,reserved,null)}
  function preReserveExactPlans(plans,reserved){plans.forEach(p=>{if(p.range&&p.days.length)reserve(reserved,findExactSlot(p.days[0],p.range))})}
  function pLabel(i,date){return date===ymd(new Date())?'Today':DAY_LABELS[i]}
  function addBlock(state,plan,slot,idx,total,made){const title=plan.title+(total>1?' pt. '+idx:'');state.custom.push({id:'ai-'+Date.now()+'-'+made+'-'+idx,date:slot.date,start:hm(slot.start),end:hm(slot.end),title,type:plan.type||'personal',source:'custom',aiCreated:true,aiDetails:true,aiGrouped:!!plan.grouped,aiExact:!!(plan.range&&plan.days.length),taskIds:plan.taskIds,taskTexts:plan.taskTexts})}
  function markTaskDays(plan,parts){const labels=uniq(parts.map(p=>pLabel(p.dayIndex,p.date)));plan.items.forEach(item=>{const current=item.task.__posScheduledLabels||[];item.task.__posScheduledLabels=uniq(current.concat(labels));item.task.assigned=true;item.task.day=item.task.__posScheduledLabels.join(', ')})}
  function cleanupTempTaskFields(tasks){tasks.forEach(t=>{try{delete t.__posScheduledLabels}catch(e){}})}
  function resolveExactOverlaps(state){const exact=(state.custom||[]).filter(c=>c.aiCreated&&c.aiExact);if(!exact.length)return;state.custom=(state.custom||[]).filter(c=>{if(exact.includes(c))return true;if(!exact.some(e=>overlaps(c,e)))return true;if(isGeneratedBlock(c))return false;return !/wind|routine/i.test(String((c.type||'')+' '+(c.title||'')))})}
  function domLooksLikeExact(el,exactBlock,probe){if(!(probe.date===exactBlock.date&&mins(probe.start)===mins(exactBlock.start)&&mins(probe.end)===mins(exactBlock.end)))return false;const txt=cleanText(el.textContent||'').toLowerCase();const labels=[exactBlock.title].concat(exactBlock.taskTexts||[]).map(cleanTitle).concat(exactBlock.taskTexts||[]).map(x=>cleanText(x).toLowerCase()).filter(Boolean);return labels.some(label=>txt.includes(label.slice(0,Math.min(18,label.length))))}
  function hideDomOverlapsWithExact(){document.querySelectorAll('[data-pos-hidden-by-exact]').forEach(el=>{el.style.display='';delete el.dataset.posHiddenByExact});const exact=(getStateObj().custom||[]).filter(c=>c.aiCreated&&c.aiExact);if(!exact.length)return;document.querySelectorAll('#scheduleView .day').forEach((dayEl,dayIndex)=>{const date=ymd(addDays(scheduleMonday(),dayIndex));dayEl.querySelectorAll('.event').forEach(el=>{const txt=(el.querySelector('.time')||{}).textContent||'';const m=txt.match(/(\d{1,2}:\d{2})\s*-\s*(\d{1,2}:\d{2})/);if(!m)return;const probe={date,start:m[1],end:m[2]};if(exact.some(e=>domLooksLikeExact(el,e,probe)))return;if(exact.some(e=>overlaps(probe,e))){el.dataset.posHiddenByExact='1';el.style.display='none'}})})}
  function allocate(){const state=getStateObj();let tasks=getTasks();extractVisibleTasks(tasks);tasks=syncTasksToVisibleOrder(tasks);const released={};(state.custom||[]).forEach(c=>{if(isGeneratedBlock(c))(c.taskIds||[]).forEach(id=>released[id]=1)});tasks.forEach(t=>{if(released[t.id])t.assigned=false;try{delete t.__posScheduledLabels}catch(e){}});state.custom=(state.custom||[]).filter(c=>!isGeneratedBlock(c));exposeState(state);const items=buildTimedItems(lastVisibleRows);if(!items.length){setStateObj(state);cleanupTempTaskFields(tasks);saveTasks(tasks);forceDomTaskOrder();try{if(typeof render==='function')render()}catch(e){}hideDomOverlapsWithExact();toastMsg('No timed tasks to schedule');return 0}const plans=buildPlansStrictVisibleOrder(items),reserved={};preReserveExactPlans(plans,reserved);let made=0,failedPlan=null;for(const plan of plans){const parts=placePlan(plan,state,reserved);if(!parts.length){failedPlan=plan;break}parts.forEach((slot,i)=>addBlock(state,plan,slot,i+1,parts.length,made++));markTaskDays(plan,parts)}resolveExactOverlaps(state);cleanupTempTaskFields(tasks);setStateObj(state);saveTasks(tasks);try{if(typeof render==='function')render()}catch(e){}try{if(typeof renderTasks==='function')renderTasks()}catch(e){}setTimeout(()=>{forceDomTaskOrder();hideDomOverlapsWithExact();try{if(typeof window.personalOSInjectDetails==='function')window.personalOSInjectDetails()}catch(e){}},0);if(failedPlan)toastMsg(made?'Created '+made+' scheduled block'+(made===1?'':'s')+'; stopped before lower-priority tasks because no valid slot was found for '+failedPlan.title:'No free time found for '+failedPlan.title);else toastMsg(made?'Created '+made+' scheduled block'+(made===1?'':'s'):'No free time found for timed tasks');return made}
  function shiftFromNext(minutes){const state=getStateObj(),today=ymd(new Date()),n=nowMin(),blocks=(state.custom||[]).filter(c=>isGeneratedBlock(c)&&c.date>=today).sort((a,b)=>String(a.date).localeCompare(String(b.date))||mins(a.start)-mins(b.start)),startIndex=blocks.findIndex(c=>c.date>today||mins(c.end)>n);if(startIndex<0)return 0;for(let i=startIndex;i<blocks.length;i++){blocks[i].start=hm(mins(blocks[i].start)+minutes);blocks[i].end=hm(mins(blocks[i].end)+minutes)}setStateObj(state);try{if(typeof render==='function')render()}catch(e){}setTimeout(hideDomOverlapsWithExact,0);return blocks.length-startIndex}
  function runFromButton(e){if(e){e.preventDefault();e.stopPropagation();if(e.stopImmediatePropagation)e.stopImmediatePropagation()}const n=allocate();if(!n)toastMsg('No free time found for those tasks');return false}
  function bindButton(){const b=document.getElementById('posAllocateTasks');if(!b||b.dataset.posReliableSchedulerCapture===VERSION)return;b.dataset.posReliableSchedulerCapture=VERSION;b.addEventListener('click',runFromButton,true)}
  function assertBinding(){window.personalOSSchedulerVersion=VERSION;window.personalOSReliableScheduleTasks=allocate;window.personalOSUpdateSchedule=allocate;window.personalOSShiftNextScheduledTask=shiftFromNext;window.__posSchedulerOverrideReady=true;bindButton();installTaskReorder();hideDomOverlapsWithExact()}
  assertBinding();
  setInterval(assertBinding,500);
})();
